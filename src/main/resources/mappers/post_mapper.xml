<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korit.mini_post.mapper.PostMapper">

    <resultMap id="postResultMap" type="com.korit.mini_post.entity.Post">
        <id property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content" />
    </resultMap>


    <insert id="insertPost" keyProperty="postId" useGeneratedKeys="true">
        insert into post_tb
        values(default, #{userId}, #{title}, #{content}, NOW(), NOW())
    </insert>

    <select id="selectAllPost" resultMap="postResultMap">
        select
            post_id,
            user_id,
            title,
            content
        from
            post_tb
    </select>

    <select id="selectPostById" resultMap="postResultMap">
        select
            post_id,
            user_id,
            title,
            content
        from
            post_tb
        where
            post_id = #{postId}
    </select>

</mapper>